spring:
  application:
    name: api-starter


app:
  key:
    ${APP_KEY}

server:
  port: ${PORT_STARTER}
  address: ${HOST_STARTER}

watcher:
  api:
    url:
      v1: http://${WATCHER_HOST}:${WATCHER_PORT}/api/v1
